[pytest]
DJANGO_SETTINGS_MODULE = mimir.settings.test
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
log_file = tests.log
log_file_level = DEBUG
log_file_format = %(asctime)s [%(levelname)-8s] [PID:%(process)d:TID:%(thread)d] [%(pathname)s:%(lineno)d] [%(module)s.%(funcName)s] %(message)s
log_file_date_format = %Y-%m-%d %H:%M:%S
asyncio_mode = auto
addopts =
    --tb=short
    --strict-markers
    -v
    --reuse-db
    --ignore=tests/e2e
testpaths = tests
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests (requires --browser chromium flag)
    slow: Slow running tests
asyncio_default_fixture_loop_scope = function
